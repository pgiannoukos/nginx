pipeline {
    agent any
    // agent {
    //     label "main"
    // }
    stages {
        stage('test') {
            steps {
                sh "docker ps"
                sh "docker ps"
            }
        }
    }
}